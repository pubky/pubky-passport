# Identity Core

Defines the runtime-neutral public Pubky identity shown across Passport. Browser-owned
key handles, sessions, and secret material live in `src/browser/pubky/ports.ts`;
browser-local identity summaries live behind the browser identity controller.
