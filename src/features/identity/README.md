# Identity Feature

Defines provider-neutral public identities, opaque browser key handles, key material,
and verified provider identities. Browser code owns Drive, crypto, and Pubky SDK
work; server code owns token verification and wrapping-key derivation.
