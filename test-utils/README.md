# Test Utilities

Test-only helpers, fakes, and architecture checks. Fakes record only safe metadata.
Architecture tests enforce the runtime boundaries; update them with intentional
top-level changes.
